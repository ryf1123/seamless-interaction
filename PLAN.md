# seamless-interaction · 本地 text → gesture 计划

目标不是刷 benchmark，是**亲手把说话人手势生成的每一块拆开看懂**：
动作用什么表示、语音怎么变成条件、文本在哪一步真正起作用、
为什么这个任务是「多对多」的所以确定性回归一定会输、长序列怎么接、
表情和手势该联合建模还是级联。

机器：M4 10 核 / 16 GB，无 CUDA。PyTorch MPS + macOS `say`。

一句话的系统：**每 1/30 秒，模型看这一帧的语音特征和这一帧正在说的词，
生成 43 个上半身关节的 6D 旋转；整段用 flow matching 一次性去噪出来。**

---

## 0. 表示与数据（✅ 已完成）

- `si/rotation.py` 6D 旋转互转；`si/skeleton.py` SMPL-H 52 关节树 + 上半身 43 关节 ×6D = **258 维**
  （与 Seamless Interaction §4.1 完全同维度）；`si/pose.py` 29 个有名字的控制自由度 ↔ 258 维。
- `si/corpus.py` 带类型槽位的模板语料，13 个语义手势类别；
  `si/tts.py` macOS `say` **逐词合成 + 拼接**，词级对齐精确到帧。
- `si/gesture_expert.py` 规则专家：idle 摇摆 + 语音节拍驱动的下砍 + 词触发的成形手势。
- `si/render.py` 火柴人渲染（正视 + 侧视 + 波形 + 词条）。

验收：`python -m si.dataset 400` 产出 400 句 / 5.9 万帧 / 32.6 分钟；
`docs/figs/00_pipeline.png` 一图看完整条链路。

**要体验的坑**：`say` 整句合成拿不到词级时间戳；逐词合成能拿到，代价是韵律不自然。
真实数据里这一步是 WhisperX 强制对齐（论文 §3.4）。

## 1. 第一环：生成式 vs 确定性（第一次跑完，结论被推翻，重跑中）

同一个 DiT 骨干、同一份条件，只换目标函数：
`objective=flow`（Seamless Interaction 的 flow matching）vs `objective=regress`（L1 回归）。

**第一次的结果（2026-08-24）：回归完胜，SemAcc 98.5% vs 64.2%，MPJPE 3.86 vs 11.21 cm。**
原因不在模型，在数据：确定性版数据给定 (文本, 语音) 后动作几乎唯一
（条件变异 3.37 cm），回归的 3.86 cm 已经贴着噪声底，而 flow 生成的变异是真实变异的 28 倍。
详见 `notes/05`。

修法是改数据不是改模型：`si/gesture_expert.py` 加了 `omit_p` / `mirror_p` / `amp_jitter`
三个旋钮，`python -m si.dataset 400 --multimodal` 产出 `data/toy_multi`，
语义峰值帧的条件变异从 2.32 cm 涨到 25.12 cm。重跑套件是 `objective_multi`。

验收（重跑后）：flow 组 FGD 更低、SemAcc 不低于回归、Diversity 接近数据真实变异
（而不是 28 倍）；同屏对比视频里回归那一路应该是「手抬到一半」的模糊姿态。

## 2. 第二环：文本到底有没有在起作用（靶心）

四组对照，只动 `text_mode`：

| 组 | 在问什么 | 预期 |
|---|---|---|
| `seq` 逐帧词 id | 主基线 | SemAcc 最高 |
| `shuffle` 句内换位 | 时机对、词不对 | 手势时机对但类别错 |
| `bow` 整句词袋 | 词对、时机不对 | 手势对但打在错的时刻 |
| `none` 没有文本 | 只有语音 | SemAcc 塌到接近 1/13 |

产出：**同一条语音、只改文本条件，人物做出不同手势**的同屏对比视频 —— 全项目最直观的一张图；
以及 13×13 的语义混淆矩阵。

## 3. 第三环：语音表示

`mel`（80 维连续）vs `token`（k-means 离散，12.5 fps 重采样到 30 fps，复刻论文 §4.1 的帧率不匹配）
vs `env`（1 维包络）vs `none`。看节拍对齐掉多少、语义命中率变不变。

关键提醒：**抖动的动作会刷高 BeatAlign**（DiffSHEG 和 Seamless Interaction 都提醒过），
所以这一环必须同时看 FGD 和视频。

## 4. 第四环：长序列（FOPPAS）

`si/flow.py` 里已实现 outpainting 分段采样。第一次测量（8 条长句，2026-08-24）
的结论是：**接缝在统计上看不出来，连 overlap=0 的纯拼接都看不出来**——
但原因不是 FOPPAS 做得好，是**生成动作全段都抖**，|Δv| 中位数比真值高两个数量级，
接缝被自身的抖动淹没了。详见 `notes/02`。

所以这一环的顺序要倒过来：
1. **先把抖动压下去**：λ_v 扫 1 / 3 / 10、加 DiffSHEG 的 Huber 重建损失（式 7）、
   训练步数扫一遍。指标看 |Δv| 中位数和 FGD，不看 BeatAlign（它对抖动不敏感）。
2. 抖动降到能看出接缝之后，再回来量 overlap = 0 / 4 / 8 / 16。
3. 位置编码截断后生成更短片段能不能用（DiffSHEG §3.5）。
4. 分段 vs 一次性生成的 FGD 差多少。

## 5. 第五环：表情 + 手势，联合还是级联

- 联合：一个模型出 258 + 137 = 395 维（Seamless Interaction 的 Joint Face+Body）。
- 级联：Face2Body / Body2Face（论文 §4.3）。
- DiffSHEG 的单向 expression→gesture 信息流 + 梯度截断，做 4 组消融
  （w/o x̂₀ᴱ、w/o detach、朴素拼接、反向），复现论文表 1 的结论方向。

本项目的表情表示是「6 个已知因子经固定随机正交基打到 128 维」，
所以可以直接问「模型学到的到底是哪个因子」——真实 Imitator 隐编码做不到这一点。

## 6. 第六环：双人（dyadic）（数据侧 ✅，训练待跑）

Seamless Interaction 的核心设定：条件里加上**对话者**的语音（论文表 14 的
Monadic A1 vs Dyadic A1+A2 那一栏量的就是这个）。

数据侧已就位（`si/dyadic.py`）：120 段 A/B 轮流说话的对话、41.4 分钟、
1825 个反馈动作，本机 14 秒能重建。设计上把问题做成了可证伪的：

- **说话方**：正常的 idle + beat + semantic 三路。
- **倾听方**：idle + **backchannel 反馈动作**——在对方的语句停顿处（高概率）
  和对方说话的重音处（低概率）点头，附带轻微耸肩和身体前倾。**没有任何语义内容。**
- 倾听时自己那一路音轨是静音的（自检里有这条不变量），所以
  **只给自己的语音，模型不可能生成出这些点头**。

要跑的实验：
1. Monadic（只给 A1）vs Dyadic（A1+A2），指标是**反馈动作的时间对齐**
   （生成的点头峰值到最近一个真值反馈事件的距离）。预期 Monadic 基本为零。
2. 加上对方的视觉条件（对方的 SMPL-H）——论文的 AV Dyadic。
3. 抢话（overlap）时的行为：两个人都在说话时，条件里两路都非静音。

`python scripts/explain_dyadic.py` 出下面这张图。

## 6.5 手指保真度（新增，来自第一批结果）

`count1/2/3` 三个只差手指的类别，原型间距只有 0.33 cm（全体中位数 5.5 cm），
在「平均关节位置距离」下几乎不可分。这和 Seamless Interaction §3.1 遇到的是同一件事：
HMR 2.0 的身体重建给出的手是平的，他们不得不额外跑 ViTPose + HaMeR 再把手接回手腕。

要做的：
- 评测侧：加一个**只看手指**的指标（手指关节相对腕部的位置距离），单独报。
- 模型侧：在损失里给手指关节单独一项权重，扫 1 / 3 / 10，看手指保真度和整体 FGD 怎么权衡。
- 数据侧：不改。真人的数手指手势本来就只差手指，把它人为拉开会让这个实验失去意义。

## 7. 第七环：泛化边界

没见过的词、没见过的词序、没见过的说话人、比训练窗口长得多的句子、
语义手势的稀有度（把某一类在训练集里的比例压到 1%，看它还学不学得到 —— 这直接对应论文
§4.4.3 说的长尾问题）。产出一张失效边界表。

## 明确不做

真机/真人渲染（2D 视频生成、3D Codec Avatar）、需要 CUDA 的训练、
下载 4000 小时的真实数据集全量、语音 LLM 集成。

## 三个提醒

- **SemAcc 是主指标**。它是唯一能回答「文本有没有用」的数，其他指标都只是参考。
- **任何一个新数据版本，先量噪声底**：同一条件换随机种子 K 次、两两算距离。
  不知道下限就读不懂模型的分数——第一环就是这么栽的。
- **BeatAlign 和 Diversity 只在动作平滑时才有意义**，抖动会同时刷高这两个数。
- **每一环一页 notes**，这是这个项目真正的产出。
